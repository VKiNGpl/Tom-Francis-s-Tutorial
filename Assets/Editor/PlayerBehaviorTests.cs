using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using NUnit.Framework;

public class PlayerBehaviorTests
{
    [Test]
    public void CalculatePlayerSpeed_Test()
    {
        //ARRANGE
        /*
         * var speedCalculator = new SpeedCalculator();
         * var killedEnemies = 24;
         * var killedVillagers = 3;
         * var multiplicator = 2;
         * var expectedPoints = (24 * 100 + 2 * 500 - 3 * 100) * 2;
         */

        //ACT
        /*
         * var points = pointCalculator.CalculateTotalPoints(
         * killedEnemies,
         * killedVillagers,
         * multiplicator;
         */

        //ASSERT
        /*
         * Assert.That(points, Is.EqualTo(expectedPoints));
         */
    }
}
